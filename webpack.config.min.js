
const path = require('path');
const uglify = require('uglifyjs-webpack-plugin');

module.exports = {
    mode: "development",
    entry: "./src/browser.js",
    module: {
        rules: [
            {
                test: /\.js$/,
                use: "babel-loader",
                exclude: /node_modules/
            }
        ]
    },
    plugins: [
        new uglify()
    ],
    output: {
        path: path.resolve(__dirname, "dist"),
        filename: 'ess-sdk.min.js',
        library: 'EssSdk'
    },
    node: {
        fs: 'empty'
    }
}